# Bitácora — Sesión 2026-07-04 (scaffold inicial)

## Qué se hizo
- **[H-000] Scaffold del sistema** completado: estructura del repo, `CLAUDE.md` (carta operativa), `PLAN-MAESTRO.md`, tablero Kanban con 10 tarjetas, 5 specs de pipeline, `config/ecosistema.example.json`, y los scripts `monitor-vps.ps1` y `generar-reporte.ps1`.

## Evidencia
- Prueba de humo de ambos scripts contra la config de ejemplo:
  - `monitor-vps.ps1` ejecutó las 4 fases (ping, puertos TCP, SSH omitido según config, HTTP), escribió `estado/status-*.json` y salió con código 2 (CRÍTICO, esperado: hosts ficticios).
  - `generar-reporte.ps1` generó el reporte Markdown correcto desde ese status (semáforo, tabla de críticos, detalle completo).
  - Artefactos de prueba borrados tras la verificación para que `estado/` y `reportes/` arranquen limpios.

## Incidencias y correcciones
1. **Encoding**: los `.ps1` se escribieron inicialmente en UTF-8 sin BOM; PowerShell 5.1 los lee como ANSI y el byte `0x94` del guion largo `—` se interpreta como comilla `”`, rompiendo el parseo. **Fix:** guiones largos eliminados de los scripts + ambos archivos re-codificados a UTF-8 con BOM. *Regla para el futuro: todo `.ps1` de este repo debe guardarse en UTF-8 con BOM y evitar `—` en código.*
2. **Escapes**: `\"` y `\$` no escapan en cadenas PowerShell (eso es bash). El comando remoto de SSH se reescribió como cadena de comillas simples sin comillas dobles internas (`awk '{print int(...)}'` en vez de `printf "%.0f"`).

## Bloqueos / necesita al usuario
- **H-001**: rellenar `config/ecosistema.json` con los datos reales (IPs Tailscale de los VPS, servicios, URLs de Market Castilla, scrapers, APIs). Sin esto ningún pipeline puede correr contra infraestructura real.

## Siguiente paso
Con H-001 hecho → ejecutar H-002 (verificación de acceso) y H-003 (primer barrido real del monitor).

---

# Adenda — misma fecha, segunda parte de la sesión

## Diagnóstico corregido (aporte del usuario)
- **Hay un solo Hermes**: el agente de Nous Research usado **desde el panel web** (mismo que trabajó en el proyecto Dona). Su sandbox remoto no ve este PC ni el tailnet.
- La app de escritorio está descargada pero **nunca usada**; `hermes-poc` es un proyecto en el que Hermes trabajó, no el agente.
- División de trabajo adoptada: Hermes (web) = estratega/brazo en internet; Claude Code (PC) = ejecutor local. Documentado en README.

## Qué se hizo (H-011, elegido por el usuario: GitHub + app de escritorio)
- Repo git inicializado, commit inicial (19 archivos) y **repo privado publicado**: `https://github.com/celestinojbm/entrenamiento-hermes` (cuenta celestinojbm, push de `main` verificado).
- `.gitignore` con nota: `config/ecosistema.json` se versiona a propósito (repo privado, sin secretos).
- Skill para la app de escritorio dejada como propuesta en `integracion\hermes-skill\`.

## Bloqueos
- El clasificador de permisos **denegó dos veces** instalar la skill en `~\.hermes\skills\` (persistencia en la config de otro agente): debe ejecutarlo el usuario — comando en `integracion\README.md`.

---

# Adenda — tercera parte: repo público, inventario real y primer barrido

## Decisión del usuario
- Hacer el repo **público** (para que Hermes lo clone sin token y sea revisable).

## Qué se hizo
1. **Seguridad primero**: antes de publicar, se separó *framework* (público) de *datos operativos* (solo local). `.gitignore` ahora excluye `config/ecosistema.json`, `estado/*` y `reportes/*`. Regla de oro documentada en README y CLAUDE.md: ninguna IP/secreto en archivos versionados; nodos por nombre.
2. **Repo publicado**: `https://github.com/celestinojbm/entrenamiento-hermes` es **PÚBLICO**. Verificado que el inventario real NO está rastreado (`git check-ignore` OK).
3. **H-001 (inventario)**: descubiertos 2 VPS vía `tailscale status` (`hermes-dona-vps`, `openclaw-vps`) y creado `config/ecosistema.json` local con ambos + SSH:22. Faltan los `TODO` (usuarios SSH, puertos HTTP, Market Castilla, scrapers).
4. **H-003 (primer barrido real)**: monitor ejecutado contra los 2 VPS por Tailscale → **ambos OK** (ping + SSH). Reporte generado.

## Incidencia corregida
- El monitor daba falso **CRÍTICO** al intentar resolver el placeholder `"TODO"` de Market Castilla como URL. **Fix:** función `Test-Placeholder` que omite valores vacíos o que empiezan por `TODO`. Reverificado: estado global **OK**. (Script re-codificado a UTF-8 BOM tras la edición, según la regla del proyecto.)

## Pendiente del usuario
- Instalar la skill en la app de escritorio (comando en `integracion\README.md`).
- Completar los `TODO` de `config/ecosistema.json`.
- En el panel web: pedir a Hermes que clone el repo público y trabaje el tablero.

---

# Adenda — cuarta parte: ejecución desde Hermes local

## Qué se hizo
1. **H-011 avanzado**: skill `entrenamiento-hermes` instalada y verificada en `~/.hermes/skills/entrenamiento-hermes`. El listado local muestra también la skill `personas`.
2. **H-008 completada**: `pipelines/01-monitor-infraestructura.md` ahora define nodo/servicio caído, endpoint degradado/caído, disco/memoria en riesgo y el significado operativo de `OMITIDO`.
3. **H-006 completada**: `scripts/generar-reporte.ps1` ahora eleva los chequeos `OMITIDO` a una sección explícita de "Pendiente de inventario / acceso". Ejecutado correctamente contra el primer status real.
4. **H-007 completada**: creada tarea de Windows Task Scheduler `EntrenamientoHermes\MonitorInfraestructura` cada 15 minutos. Verificada con ejecución manual y último resultado `0`.
5. **Inventario local mejorado**: `config/ecosistema.json` local ya tiene el sufijo MagicDNS real del tailnet. El archivo sigue git-ignored.

## Evidencia
- `skill_view(entrenamiento-hermes)` devolvió `readiness_status: available`.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File ...\generar-reporte.ps1` generó `reportes/reporte-2026-07-04-2323.md` con estado global OK y 2 omitidos documentados.
- `schtasks.exe /Query /TN "EntrenamientoHermes\MonitorInfraestructura" /V /FO LIST` mostró tarea habilitada, próxima ejecución programada y último resultado `0` tras `/Run`.
- Se generó `estado/status-2026-07-04-2324.json` por ejecución manual de la tarea programada.
- `git check-ignore` confirmó que `config/ecosistema.json`, `estado/status-*.json` y `reportes/reporte-*.md` siguen excluidos del repo público.

## Bloqueos / pendiente
- **H-001 sigue parcial**: faltan usuarios/servicios HTTP reales de `hermes-dona-vps`, URLs de Market Castilla, scrapers y APIs externas.
- Chequeo de recursos vía SSH: `openclaw-vps` aceptó SSH como `root`; `hermes-dona-vps` pidió aprobación interactiva de Tailscale SSH, así que no se activó `ssh_habilitado` globalmente.
- Si el panel web sigue mostrando Gateway detenido, el siguiente paso humano es pulsar **Reiniciar Gateway**.

---

# Adenda — quinta parte: playbooks H-009 y autorización SSH pendiente

## Qué se hizo (sesión Claude Code)
1. Verificado que las ediciones de Hermes local conservan el BOM UTF-8 en ambos scripts. Su trabajo (H-006/H-007/H-008, mejora de reportes con sección OMITIDO, tailnet en config) se conserva y se publica en este commit.
2. **H-009 (parte de construcción)**: creados y validados con el parser de PowerShell (0 errores):
   - `scripts/playbooks/reiniciar-servicio.ps1` — reinicia un servicio systemd por SSH y re-verifica; registra en bitácora.
   - `scripts/playbooks/limpiar-disco.ps1` — **dry-run por defecto**; el borrado real exige `-Confirmar` y solo toca `rutas_limpieza_segura` del inventario.
3. Confirmado desde este PC: SSH por identidad Tailscale a `hermes-dona-vps` como `root` respondió sin clave (whoami/hostname OK).

## Bloqueo de permisos (importante)
- El clasificador de Claude Code **denegó continuar usando SSH** hacia los VPS en modo automático: exige autorización explícita del usuario nombrando los nodos. Pedida en el chat. Cuando se autorice: descubrir servicios reales en ambos VPS (puertos, systemd, docker, cron) para completar H-001, activar `ssh_habilitado:true` y probar los playbooks.

---

# Adenda — sexta parte: SSH autorizado, inventario completo, barrido 15/15 OK

## Autorización del usuario
- SSH root (identidad Tailscale) autorizado explícitamente a **ambos** VPS, solo lectura para descubrimiento; nada destructivo sin preguntar.

## Qué se hizo
1. **Descubrimiento en ambos VPS** (hostname, recursos, puertos, systemd, docker, cron): en `hermes-dona-vps` corre el **gateway de Hermes** como servicio systemd + un node local; en `openclaw-vps` corre el **gateway de OpenClaw** (proceso, no systemd) + 2 automatizaciones Dona en cron. Sin docker en ninguno. Detalles con IPs/rutas: solo en el inventario local.
2. **Proyectos cloud localizados vía GitHub** y verificados con HTTP 200: Market Castilla (Vercel), Dona, Dona Founders y Manos Venezuela. URL de Fluvia pendiente. URLs exactas: solo en inventario local.
3. **H-001 COMPLETADA**: `config/ecosistema.json` con ambos VPS (`ssh_habilitado:true`, usuario root), servicios systemd/procesos a vigilar, 4 endpoints cloud, automatizaciones y proyectos cloud.
4. **H-012 (mejora del monitor)**: chequeos nuevos de servicios systemd (`systemctl is-active`) y procesos (`pgrep -f`) vía SSH, `StrictHostKeyChecking=accept-new` (TOFU, razonable en tailnet privado) y ALERTA explícita si SSH no devuelve salida. **Barrido final: 15/15 OK, exit 0.**
5. **H-009 probado**: `limpiar-disco.ps1` en dry-run real contra `openclaw-vps` (40M liberables medidos; se negó a borrar sin `-Confirmar`). Reporte ejecutivo regenerado: 0 críticos, 0 alertas.

## Incidencias corregidas
1. **ACL rota en `~\.ssh`**: la carpeta solo daba acceso a SYSTEM; ni el propio usuario podía leer `known_hosts` (causa de los fallos intermitentes de SSH). Fix: `icacls /grant` al usuario.
2. **Host key de `openclaw-vps` sin guardar** (consecuencia de lo anterior): falso CRÍTICO del proceso del gateway. Fix: `accept-new` en el monitor + ACL corregida. Artefacto con falso crítico eliminado.
3. **`$PSScriptRoot` vacío en `param()`** (PS 5.1, playbooks): rutas calculadas ahora en el cuerpo con fallback a `$MyInvocation`.

## Pendiente
- Probar `reiniciar-servicio.ps1` con un servicio no crítico (coordinar con el usuario: reiniciar el gateway de Hermes lo interrumpiría).
- H-004 (auditar automatizaciones Dona), H-005 (flujos funcionales de Market Castilla), URL de Fluvia, APIs externas.
